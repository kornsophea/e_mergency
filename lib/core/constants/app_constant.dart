import 'dart:ui';

class AppConstant {
  AppConstant._internal();
  static const String appName = 'Flutter MVVM Boilerplate';

  static const supportedLocales = [
    Locale('en'),
    Locale('km'),
  ];

  static const fallbackLocale = Locale('en');
}
